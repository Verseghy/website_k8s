apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: static-images
spec:
  host: verseghy-gimnazium.net
  path: '/images'
  to:
    kind: Service
    name: static-svc
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None